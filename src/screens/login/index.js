import { Text, View } from 'react-native';

const LoginScreen = () => {
  return (
    <View>
      <Text style={{ fontSize: 25, textAlign: 'center', marginTop: 40, marginBottom: 40 }}>Login page</Text>
    </View>
  );
};

export default LoginScreen;
